---
import MainLayout from "../../../../layouts/MainLayout.astro";
import { articles } from "../../../../data/articles";

const selfworthArticles = articles.filter(
  (article) => article.lang === "ar" && article.link.includes("/selfworth/"),
);
---

<MainLayout
  title="تقدير الذات وحب النفس | Mind Healing"
  description="اكتشف مقالات عن تقدير الذات وحب النفس واحترام الذات والتخلص من طلب الاهتمام وبناء ثقة داخلية أقوى."
>
  <div class="container">
    <div class="category-hero">
      <h1>💖 تقدير الذات وحب النفس</h1>

      <p class="page-desc">
        اكتشف مقالات تساعدك على رفع تقديرك لذاتك، بناء حب النفس، احترام نفسك
        وحدودك، والتخلص من الحاجة المستمرة للقبول والاهتمام من الآخرين.
      </p>
    </div>

    <div class="search-box">
      <input
        type="text"
        id="searchInput"
        placeholder="ابحث داخل مقالات العقلية..."
      />

      <button id="clearSearch"> ✕ </button>
    </div>

    <div class="section">
      <div class="grid" id="articles">
        {
          selfworthArticles.map((article) => (
            <div class="card">
              <h3>{article.title}</h3>

              <p>{article.desc}</p>

              <a href={article.link}>اقرأ المقال →</a>
            </div>
          ))
        }
      </div>
    </div>
  </div>

  <script define:vars={{ selfworthArticles }}>
    const container = document.getElementById("articles");

    const searchInput = document.getElementById("searchInput");

    const clearBtn = document.getElementById("clearSearch");

    function renderArticles(filtered = selfworthArticles) {
      container.innerHTML = filtered
        .map(
          (article) => `

      <div class="card">

        <h3>${article.title}</h3>

        <p>${article.desc}</p>

        <a href="${article.link}">
          اقرأ المقال →
        </a>

      </div>

    `,
        )
        .join("");
    }

    searchInput.addEventListener("input", () => {
      const value = searchInput.value.toLowerCase();

      clearBtn.style.display = value ? "block" : "none";

      const filtered = selfworthArticles.filter((article) => {
        return (
          article.title.toLowerCase().includes(value) ||
          article.desc.toLowerCase().includes(value)
        );
      });

      renderArticles(filtered);
    });

    clearBtn.addEventListener("click", () => {
      searchInput.value = "";

      clearBtn.style.display = "none";

      renderArticles();
    });
  </script>
</MainLayout>
