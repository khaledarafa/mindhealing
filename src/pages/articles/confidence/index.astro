---
import MainLayout from "../../../layouts/MainLayout.astro";
import { articles } from "../../../data/articles";

const confidenceArticles = articles.filter(article =>
  article.link.includes("/confidence/")
);
---

<MainLayout
  title="Confidence Articles | Mind Growth"
  description="Explore confidence articles about self belief, overthinking, insecurity, social confidence, and personal growth."
>

<div class="container">

  <div class="category-hero">

    <h1>
      💪 Confidence & Self Belief
    </h1>

    <p class="page-desc">
      Explore articles about confidence,
      self belief,
      overthinking,
      social anxiety,
      and personal growth.
    </p>

  </div>

  <div class="search-box">

    <input
      type="text"
      id="searchInput"
      placeholder="Search confidence articles..."
    />

    <button id="clearSearch">
      ✕
    </button>

  </div>

  <div class="section">

    <div class="grid" id="articles">

      {
        confidenceArticles.map(article => (

          <div class="card">

            <h3>{article.title}</h3>

            <p>{article.desc}</p>

            <a href={article.link}>
              Read More →
            </a>

          </div>

        ))
      }

    </div>

  </div>

</div>

<script define:vars={{ confidenceArticles }}>

const container = document.getElementById("articles");

const searchInput = document.getElementById("searchInput");

const clearBtn = document.getElementById("clearSearch");

function renderArticles(filtered = confidenceArticles) {

  container.innerHTML = filtered.map(article => `

    <div class="card">

      <h3>${article.title}</h3>

      <p>${article.desc}</p>

      <a href="${article.link}">
        Read More →
      </a>

    </div>

  `).join("");

}

searchInput.addEventListener("input", () => {

  const value = searchInput.value.toLowerCase();

  clearBtn.style.display =
    value ? "block" : "none";

  const filtered = confidenceArticles.filter(article => {

    return (

      article.title.toLowerCase().includes(value)

      ||

      article.desc.toLowerCase().includes(value)

    );

  });

  renderArticles(filtered);

});

clearBtn.addEventListener("click", () => {

  searchInput.value = "";

  clearBtn.style.display = "none";

  renderArticles();

});

</script>

</MainLayout>